<Window x:Class="RocketLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RocketLauncher"
        mc:Ignorable="d"
        Title="Rocket Launcher" Height="600" Width="1100"
        Background="#070707">
    
    <Window.Resources>
        <Style x:Key="UserBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="5 10 5 10"/>
            <Setter Property="Background" Value="#232323"/>
            <Setter Property="Cursor" Value="Hand"></Setter>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="UseLayoutRounding" Value="True"></Setter>
            <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" 
                                                To="#444444" Duration="0:0:0.15"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" 
                                                To="#232323" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="UserTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
            <Setter Property="HorizontalAlignment" Value="Center"></Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="10" Background="#070707">
        <Grid.RowDefinitions>
            <RowDefinition Height="60px"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <Grid Grid.Row="0">
            <TextBlock Text="Rocket Launcher" Foreground="White" FontSize="32" 
                       HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0"/>
        </Grid>
        
        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Accounts -->
            <StackPanel Grid.Column="0" VerticalAlignment="Top" Margin="20 20 20 20">
                <StackPanel x:Name="AccountsList" VerticalAlignment="Top"></StackPanel>
                
                <!-- Add Account -->
                <Border Style="{StaticResource UserBorderStyle}" MouseLeftButtonUp="AddAccount_Click">
                    <Border.Effect>
                        <DropShadowEffect Color="#00FFFF" BlurRadius="30" ShadowDepth="0" Opacity="0.7" RenderingBias="Quality"/>
                    </Border.Effect>
                    <TextBlock Text="Add Account" Style="{StaticResource UserTextStyle}"/>
                </Border>

            </StackPanel>
        </Grid>
        
        <Grid Grid.RowSpan="2" x:Name="LoadingPopup" Background="#80000000" Visibility="Collapsed">
            <Border Width="280" Height="150" Background="Black" BorderBrush="White" BorderThickness="4" CornerRadius="10" 
                    HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock x:Name="LoadingText" Text="Starting..." FontSize="20" Foreground="White" HorizontalAlignment="Center" Margin="0,10"/>
                    <ProgressBar IsIndeterminate="True" Width="230" Height="30"/>
                </StackPanel>
            </Border>
        </Grid>
        
    </Grid>
    
</Window>
